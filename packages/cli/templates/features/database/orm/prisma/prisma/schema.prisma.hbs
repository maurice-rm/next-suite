// Every .prisma file below prisma/ is part of this schema.

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "{{#if (eq database.engine "postgres")}}postgresql{{else}}mysql{{/if}}"
}
