---
"create-next-suite": minor
---

Map Drizzle columns to snake_case through the `casing` option instead of a
name string per column, and store the Postgres auth timestamps as
`timestamp with time zone`.
