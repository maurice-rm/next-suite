---
"create-next-suite": patch
---

Name the prop types of the generated `RootLayout`, `Providers` and
`HydrateClient` components (`RootLayoutProps`, `ProvidersProps`,
`HydrateClientProps`) instead of writing them inline.
