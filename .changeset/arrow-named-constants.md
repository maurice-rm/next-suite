---
"create-next-suite": patch
---

Generate the root layout and `closeDatabase` as arrow functions, name the
query-client and connection-pool numbers as constants, and spell out
`request` in the tRPC route handler.
